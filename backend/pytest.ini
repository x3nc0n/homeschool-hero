[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests
pythonpath = .
markers =
    integration: exercises full-stack or external-service dependent flows that can be skipped in CI
    slow: takes noticeably longer than the default unit/API suite
filterwarnings =
    ignore::DeprecationWarning
